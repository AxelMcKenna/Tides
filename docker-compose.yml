services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: tides
      POSTGRES_PASSWORD: tides_dev
      POSTGRES_DB: tides
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
